export default class Tag {
   private id: number;
   private name: string;
   private slug: string;
   private language: string;
   //number of games in that tag:
   private gamesCount: number;
   private tagBackgroundImage: URL;
}
